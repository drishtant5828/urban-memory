#include <stdio.h>
#include <stdlib.h>

int main()
{
   int a,b;
   printf("enter the two numbers");
   scanf("%d  %d", &a,&b);
   int sum=a+b;
   printf("%d + %d = %d",a,b,sum);
   return 0;
}
